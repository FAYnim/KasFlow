# Konfigurasi Koneksi Database MySQL
# Salin file ini menjadi .env lalu sesuaikan nilainya
# Catatan: file .env TIDAK boleh di-commit ke git (sudah diabaikan di .gitignore)

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=cashflow_kelas
DB_USER=root
DB_PASS=
